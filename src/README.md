This folder contains small reusable scripts used by experiments.

`train_sample.py` — minimal example that loads a pretrained model and runs a forward pass.

Usage
1. Create a virtual environment and install dependencies (see top-level `notebooks/SETUP.md`).
2. Run `python src/train_sample.py` to verify the environment.
