model_name: distilbert-base-uncased
task: sentiment
max_seq_length: 128
batch_size: 16
learning_rate: 0.00002
num_epochs: 3
warmup_steps: 100
log_every: 50
eval_every: 200
checkpoint_dir: ./runs
seed: 42
device: cpu
